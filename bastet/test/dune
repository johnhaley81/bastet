; Temporarily disabled due to dependency issues
; (tests
;  (names test_bastet)
;  (package mel-bastet)
;  (libraries alcotest qcheck qcheck-core qcheck-alcotest fmt bastet)
;  (action (run %{test})))
